spring.application.name=ms-carrito
server.port=9092

# Nivel global
logging.level.root=INFO

# Tu package
logging.level.com.example.ms_carrito=DEBUG

# Archivo de logs
logging.file.name=logs/ms_carrito.log


# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/db_carrito
spring.datasource.username=root
spring.datasource.password=

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true

# JWT (debe coincidir con ms_users)
jwt.secret=una_clave_secreta_larga_y_segura_de_al_menos_32_bytes

# Logging
logging.level.org.springframework.security=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n